# Neon PostgreSQL 连接字符串
# 格式: postgres://用户名:密码@主机名/数据库名?sslmode=require
DATABASE_URL=postgres://username:password@hostname/database?sslmode=require

# 服务器端口
PORT=3001

# 允许的跨域来源 (多个用逗号分隔，* 表示允许所有)
CORS_ORIGIN=*
